name: Deploy Workflow
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
    types: [ closed ]
  workflow_dispatch:
jobs:
  deploy_staging:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    environment: STAG
    steps:
      - uses: actions/checkout@v2
      - name: rsync file to folder
        uses: easingthemes/ssh-deploy@main
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_ALI_STAGE_PRIVATE_KEY }}
          ARGS: "-avzr --delete"
          SOURCE: ""
          REMOTE_HOST: ${{ secrets.SSH_ALI_STAGE_HOST }}
          REMOTE_USER: root
          TARGET: /var/www/mtrc
          # EXCLUDE: "/src/"
      # - name: drupalconsole update cr
      #   uses: appleboy/ssh-action@master
      #   with:
      #     host: ${{ secrets.SSH_ALI_STAGE_HOST }}
      #     username: ec2-user
      #     key: ${{ secrets.SSH_ALI_STAGE_PRIVATE_KEY }}
      #     port: 22
      #     script: |
      #       whoami
      #       cat << EOF >> /var/www/f4f_erp/sites/stag/settings.php
      #       ${{ secrets.AWS_STAG_SETTINGS }}
      #       EOF
      #       cd /var/www/f4f_erp
      #       chmod -R 777 sites/stag/files/
      #       chmod -R 777 sites/stag/files/f4f/
      #       vendor/bin/drupal --uri=stagerp.first4figures.com update:execute system
      #       vendor/bin/drupal --uri=stagerp.first4figures.com cr all
  # deploy_production:
  #   runs-on: ubuntu-latest
  #   environment: PRODUCTION
  #   needs: deploy_staging
  #   steps:
  #     - uses: actions/checkout@v2
  #     - name: rsync file to folder
  #       uses: easingthemes/ssh-deploy@main
  #       env:
  #         SSH_PRIVATE_KEY: ${{ secrets.AWS_SSH_PROD_PRIVATE_KEY }}
  #         ARGS: "-avzr --delete"
  #         SOURCE: ""
  #         REMOTE_HOST: ${{ secrets.AWS_SSH_PROD_HOST }}
  #         REMOTE_USER: ec2-user
  #         TARGET: /var/www/f4f_erp
  #         # EXCLUDE: "/dist/, /node_modules/"
  #     - name: drupalconsole update cr
  #       uses: appleboy/ssh-action@master
  #       with:
  #         host: ${{ secrets.AWS_SSH_PROD_HOST }}
  #         username: ec2-user
  #         key: ${{ secrets.AWS_SSH_PROD_PRIVATE_KEY }}
  #         port: 22
  #         script: |
  #           whoami
  #           cat << EOF >> /var/www/f4f_erp/sites/prod/settings.php
  #           ${{ secrets.AWS_PROD_SETTINGS }}
  #           EOF
  #           cd /var/www/f4f_erp
  #           chmod -R 777 sites/prod/files/
  #           chmod -R 777 sites/prod/files/f4f/
  #           vendor/bin/drupal --uri=erp.first4figures.com update:execute system
  #           vendor/bin/drupal --uri=erp.first4figures.com cr all


